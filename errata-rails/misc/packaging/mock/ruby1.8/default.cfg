# Auto-generated by the Koji build system using the following command
#
# This information is from:
#
#   https://engineering.redhat.com/trac/rcm/wiki/HowTo/BrewClientConfig
#
# koji --topdir=/mnt/redhat/brewroot \
#      --topurl=http://download.devel.redhat.com/brewroot \
#      --server=http://brewhub.devel.redhat.com/brewhub \
#      mock-config --tag=$1 \
#                  --arch=$2 \
#                  -o $CONFIGDIR/default.cfg mocknightly
#
# NOTE: This file has been customized to enable yum cache because koji generated
# mock-config is disabling all cache plugin.
# koji mock-config --help doesn't provide any options to enable the yum cache plugin.

config_opts['chroothome'] = '/builddir'
config_opts['use_host_resolv'] = False
config_opts['basedir'] = '/var/lib/mock'
config_opts['rpmbuild_timeout'] = 86400
config_opts['yum.conf'] = '[main]\ncachedir=/var/cache/yum\ndebuglevel=1\nlogfile=/var/log/yum.log\nreposdir=/dev/null\nretries=20\nobsoletes=1\ngpgcheck=0\nassumeyes=1\n\n# repos\n\n[build]\nname=build\nbaseurl=http://download.eng.bos.redhat.com/brewroot/repos/eso-rhel-6-build/latest/x86_64\n'
config_opts['chroot_setup_cmd'] = 'groupinstall build'
config_opts['target_arch'] = 'x86_64'
config_opts['root'] = 'mocknightly'
config_opts['cache_topdir'] = '/workspace/cache'

config_opts['plugin_conf']['root_cache_enable'] = False
config_opts['plugin_conf']['ccache_enable'] = False

# These settings are exactly the same as default. I place them here just in case
# we want to change them in the future.
config_opts['plugin_conf']['yum_cache_enable'] = True
config_opts['plugin_conf']['yum_cache_opts']['max_age_days'] = 30
config_opts['plugin_conf']['yum_cache_opts']['max_metadata_age_days'] = 30
config_opts['plugin_conf']['yum_cache_opts']['dir'] = "%(cache_topdir)s/%(root)s/yum_cache/"

config_opts['macros']['%_host'] = 'x86_64-koji-linux-gnu'
config_opts['macros']['%_host_cpu'] = 'x86_64'
config_opts['macros']['%vendor'] = 'Koji'
config_opts['macros']['%distribution'] = 'Koji Testing'
config_opts['macros']['%_topdir'] = '/builddir/build'
config_opts['macros']['%_rpmfilename'] = '%%{NAME}-%%{VERSION}-%%{RELEASE}.%%{ARCH}.rpm'
config_opts['macros']['%packager'] = 'Koji'


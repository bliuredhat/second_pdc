<%=
   content_popover_helper(
     yield.tr('"', "'"),
     '',
     # needs an href but the value is irrelevant.
     '#comparison',
     :manual_text => '',
     :class       => %w[fa fa-info-circle help-popover-question-mark scrollable-popover],
     :click       => true
  )
%>

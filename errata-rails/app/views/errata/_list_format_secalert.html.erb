<%#
  # This is the old style advisory list
  #
  # Not sure if anyone wants this, but
  # demonstrating how we can easily define different output formats
  #
  # See app/models/errata_filter.rb
  #
  # TODO: Grouping doesn't work here :(
  #
  #%>
<%= render "shared/bz_table", {
  :headers   => headings_for_errata_list2,
  :func      => method(:row_for_errata_list2),
  :row_items => @erratas,
} %>

<%= post_link_confirm( "Reschedule All", :reschedule_all, @tpsrun,
      'About to invalidate all current TPS results for this errata and re-add new TPS jobs to the queue. Are you sure?', :class=>'btn' ) %>

<%= post_link_confirm( "Reschedule Failing", :reschedule_all_failure, @tpsrun,
      'About to invalidate all TPS results in state BAD for this errata and re-add new TPS jobs to the queue. Are you sure?', :class=>'btn' ) %>

<%= post_link( "Check For Missing", :check_for_missing_jobs, @tpsrun, :class=>'btn' ) %>

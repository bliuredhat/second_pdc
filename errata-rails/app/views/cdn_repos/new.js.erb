$("#et_modal .modal-header h3").text("<%= new_cdn_repo_modal_title %>");
$("#et_modal .modal-body .body-content").replaceWith("<%= escape_javascript(cdn_repo_modal_body_content) %>");
$("#et_modal .modal-footer .footer-content").replaceWith("<%= escape_javascript(cdn_repo_modal_footer_content) %>");

$("#save_cdn_repo").on("click", function() {
  var form_id = $(this).data('form-id');
  $('#' + form_id).submit();
});

$("#cdn_repo_name").focus();
